# Copy to .env.local. Keep the real secret private and never use a VITE_ prefix.
ENQUIRY_SCRIPT_URL=
ENQUIRY_SCRIPT_SECRET=
# Production only: exact public origin, with no trailing slash.
# Leave unset for localhost development.
# ENQUIRY_ALLOWED_ORIGIN=https://ndrkfgc.edu.in
